"""Stage 1 benchmark construction helpers.

The builder MVP turns forward/task metadata into auditable construction
artifacts: a normalized stage1 spec, autograd-oracle guidance, LLM prompt files,
and verification commands. It does not call an LLM API.
"""
